name: Build site

on:
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: mstksg/get-package@v1
      name: Build
      run: |
        stack build
        git status

